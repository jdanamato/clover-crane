<div class="article-hero_layout">
          <h1 li-object="article.title">{{ article.title }}</h1><img li-object:src="article.image | image_url: width: 1200" li-object:alt="article.image.alt" alt="{{ article.image.alt }}" src="{{ article.image | image_url: width: 1200 }}" loading="eager" class="article-hero_image">
        </div> {% schema %} {
    "tag": "section",
    "name": "Article Hero",
    "class": "section s-pb-2",
    "presets": [
        {
            "name": "Article Hero",
            "category": "Liquify",
            "blocks": []
        }
    ],
    "enabled_on": {
        "templates": [
            "article"
        ]
    }
} {% endschema %}